
package iterator;

public interface MyIterator {
	// Interface that has 2 methods to be implemented, hasNext which checks if the
	// next iteration is available and next which returns value of next
	boolean hasNext();

	int next();
}
